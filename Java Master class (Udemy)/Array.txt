##Array
boolean array elements would be initialized to false

string or other objects would be null

ArrayIndexOutOfBoundsException: accessing index out ot range

dereference
	anotherArray = new int[]{1,2,3,4,5};//array 변형전에 정의하면  다른 공간에 있는 array를 가져옴

##ArrayList

ArrayList 는 클래스 이기 때문에 ArrayList<>(); 의 형태로 사용한다
 
groceryList(ArrayList 이름) .size(), .get(), .set(),.remove() 사용해 배열 크기, 배열 조회,배열삽입,배열삭제가능

.contains() 사용해 배열에 특정 value가 들어가있는지 확인 가능

.indexOf()사용해 배열에 속한 특정 value가 몇번째에 들어있는지 확인

예제 실습한거 어떻게 올리지...
	

