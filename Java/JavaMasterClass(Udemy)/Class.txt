reveal in finder-- 파일 생성일자 알수있는 방법...?


class:  blueprint for an object we'll gonna create
member variable :access only in the class

<access modifier>
public  : endless access to this class
private : access only in the same package,hide the fields to access outside,internal access only (hidden from view outside)